import styled from "styled-components";
import { Header } from "./components/Header";

const Wrap = styled.div``;

export const App = () => {
  return (
    <Wrap>
      <Header />
    </Wrap>
  );
};
